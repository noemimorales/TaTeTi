using System;
using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.TestTools;

public class TatetiPresenterShould
{
    private TatetiPresenter presenter;

/*    [Test]

    public void CreateAGridAtStart()
    {
        CellType[,] expectedGrid = AEmptyGrid();
        CellType[,] resultGrid = null;

        presenter = new TatetiPresenter();
        resultGrid = presenter.GetGrid();

        Assert.AreEqual(expectedGrid, resultGrid);
    }

    public CellType[,] AEmptyGrid()
        {
        return new[,]
        {
            {CellType., },
            { },
            { }
        };
        }
*/
}
